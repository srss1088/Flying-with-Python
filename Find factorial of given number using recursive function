def calc_factorial(x):
    if(x==1):
        return 1
    else:
        return(x*(calc_factorial(x-1)))
n=int(input("Enter the number:"))
print("the factorial of given numbber is:",calc_factorial(n))
